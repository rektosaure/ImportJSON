{
  "name": "importjson",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@google/clasp": "3.4.1",
    "esbuild": "0.28.2",
    "json-p3": "2.3.0",
    "re2js": "2.8.6"
  }
}
